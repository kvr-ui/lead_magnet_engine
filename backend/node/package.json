{
  "name": "wati-cleanup-server",
  "version": "1.0.0",
  "description": "Express front door for the WATI CSV cleaner. Proxies to the existing Python (app.py) backend, which keeps doing the actual CSV work.",
  "private": true,
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "http-proxy-middleware": "^3.0.0",
    "mongoose": "^9.8.0"
  }
}
